import { SlideImagePipe } from './slide-image.pipe';

describe('SlideImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SlideImagePipe();
    expect(pipe).toBeTruthy();
  });
});
